apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: taxonworks

resources:
  - namespace.yml
  - configmap.yml
  # - secret.yml  # Uncomment when you create the actual secret file
  - postgres.yml
  - pv-claims.yml
  - app.yml
  - ingress.yml

images:
  - name: sfgrp/taxonworks
    newTag: 0.3.12  # Update as needed