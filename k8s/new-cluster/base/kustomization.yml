apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: taxonworks

resources:
  # - namespace.yml  # Commented out for AKS - namespace managed separately
  - configmap.yml
  - secret.yml  # Secret file with database credentials
  - postgres.yml
  - pv-claims.yml
  - app.yml
  - ingress.yml

images:
  - name: sfgrp/taxonworks
    newTag: 0.9.8  # Updated to latest available version